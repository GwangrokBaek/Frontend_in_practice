// 식별자 : 코드 내의 변수, 함수, 혹은 속성을 식별하는 문자열
// 이때 식별자는 대소문자를 구별한다.
// 유니코드 문자(즉, 한글도 가능), $, _, 숫자(0-9) 를 사용할 수 있다. 하지만
// 숫자로 시작할 수는 없다.
// 예약어는 사용할 수 없고, 공백 문자도 사용할 수 없다.

var name = 'Mark'; // Mark 라는 값이 name 라는 식별자로 식별될 수 있다.
function hello() {} // 이 함수를 hello 라는 이름으로 식별할 수 있다.
var person = {name: 'Mark', age: 37}; // Mark 라는 값을 가지는 name 이라는 속성과, 37 이라는 값을
// age 라는 속성 즉, 식별자로 식별할 수 있다.